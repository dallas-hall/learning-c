#include <stdio.h>

int main(void)
{
	/*
	 * Note the double percent for a literal percent sign.
	 * Note the double backslash for a literal backslash.
	 * There is no exponenation syntax in C, need to use the Standard Library pow function.
	 *
	 */
	printf("# Arithmetic Operators\nModulo is %%\nMultiplication is *\nDivision is \\\nAddition is +\nSubtraction is -");
	printf("\n\n# Relational Operators\nGreater than or equal to is >=\nGreater than is >\nLess than or equal to is <=\nLess than is <\nEquals is ==\nNot equals is !=\n");
	return 0;
}
